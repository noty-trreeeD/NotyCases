import { CaseOpeningPage } from "@/pages";
import {Header} from "@/widgets";

export const App = () => {
    return (
        <div>
            <Header />
            <CaseOpeningPage />
        </div>
    )
}
